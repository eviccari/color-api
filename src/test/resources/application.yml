server:
  port: 8080
  servlet:
    contextPath: /

spring:
  datasource:
    username: root
    password: 123
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/products
    driver-class-name: com.mysql.cj.jdbc.Driver
